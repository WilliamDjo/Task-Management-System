version: '3'
services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    depends_on:
      - db
  db:
    image: mongo:latest
    volumes:
      - mongodb_data:/data/db
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"

volumes:
  mongodb_data: